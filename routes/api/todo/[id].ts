import { Handlers } from "$fresh/server.ts";

export const handler: Handlers = {
  POST(_req, _ctx) {
    return new Response();
  },
  PUT(_req, _ctx) {
    return new Response();
  },
  DELETE(_req, _ctx) {
    return new Response();
  },
};
